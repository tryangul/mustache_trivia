import { timeDiff } from '../../../helpers/time-diff';
import { module, test } from 'qunit';

module('Unit | Helper | time diff');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = timeDiff(42);
  assert.ok(result);
});
