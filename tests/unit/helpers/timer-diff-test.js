import { timerDiff } from '../../../helpers/timer-diff';
import { module, test } from 'qunit';

module('Unit | Helper | timer diff');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = timerDiff(42);
  assert.ok(result);
});
